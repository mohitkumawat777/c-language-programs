// area of the circle
#include<stdio.h>
void main(){
float pi=3.14159;
float radius,totalarea;
printf("Enter the radius of circle: ");
scanf("%f",&radius);
totalarea= pi*radius*radius;
printf("area of the circle is: %fCM^2",totalarea);
getc;
}